export const config = {
  api: {
    bodyParser: {
      sizeLimit: '1mb', // You can adjust the size as needed
    },
  },
};

// Your API handler function below
export default function handler(req, res) {
  // ... your API logic
}